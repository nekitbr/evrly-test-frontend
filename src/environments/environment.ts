// Runtime configuration for the application.
// The API base URL should be set at build time using Angular file replacements
// or by editing this file for local development.

export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:3000',
};

